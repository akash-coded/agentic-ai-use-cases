# TravelMind QA kit dependencies.
# Pinned to the versions the bootcamp environment used. Loosen with care, and
# re-run the eval after any bump (a library change can shift behaviour).

strands-agents==1.42.0       # the agent SDK (the system under test)
bedrock-agentcore==1.14.0    # AgentCore wrapper and runtime helpers
boto3>=1.34                  # AWS SDK: used for CloudWatch Logs Insights (Topic 3)
pytest>=8.0                  # test runner for Topics 1 and the pipeline
ragas>=0.2                   # evaluation metrics, collections API (Topic 2, RAGAS)
langchain-aws>=0.2           # ChatBedrock, wrapped as the RAGAS evaluator (Topic 2)

# Optional, only if you regenerate the notebooks from the generator scripts:
# nbformat>=5.9
