# Pin everything. An unpinned image is a different agent next week.
# These are the versions the kit was built and verified against.

strands-agents==1.42.0
bedrock-agentcore==1.14.0
boto3>=1.35.0

# Only needed for the model-gateway notebook (gateway_routing.ipynb).
# Safe to leave installed; not imported by the deploy path.
litellm>=1.50.0
