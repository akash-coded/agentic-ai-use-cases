# Strands teaching notebooks - dependencies (verified versions)
strands-agents>=1.42
strands-agents-tools>=0.8
mcp>=1.27
pydantic>=2
boto3>=1.34

# Optional: only needed for the A2A (agent-to-agent over HTTP) section in notebook 3
# strands-agents[a2a]
